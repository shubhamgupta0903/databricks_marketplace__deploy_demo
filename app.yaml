command: ["streamlit", "run", "app.py", "--server.port", "8000"]

env:
  - name: APP_NAME
    valueFrom:
      secretScope: my-app-scope
      secretKey: APP_NAME

  - name: API_KEY
    valueFrom:
      secretScope: my-app-scope
      secretKey: API_KEY
